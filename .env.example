# Copy to .env for httpyac / direnv. The worldcup feed uses ESPN unauthenticated,
# so no secrets are required today. Future authenticated feeds add their token here;
# `npm run secrets` pushes EVERYTHING in .env up as Worker secrets, so keep
# non-secret config out of this file.
#
# EXAMPLE_FEED_TOKEN=your_token_here
