// Worker bindings (secrets/vars). Each feed adds its own upstream token here.
// The worldcup feed uses ESPN's unauthenticated API, so there are none yet.
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Env {}
